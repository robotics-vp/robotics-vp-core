{
  "schema_version": "v1",
  "description": "Runtime wiring backlog for heuristic/advisory/sidecar surfaces that still affect production-readiness",
  "updated_at": "2026-03-26",
  "completed": [
    {
      "id": "stage1_semantic_seed_and_diffusion_routing",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - diffusion requests now carry governed hypotheses plus routing context, the diffusion stub reranks governed hypotheses before any fallback lane, and Stage-1 datapack admission now degrades heuristic/unbenchmarked video manifests into explicit shadow-only work orders instead of pretending benchmark readiness."
    },
    {
      "id": "scene_tracks_passthrough_truthiness",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - replay ingest and bootstrap semantic workcell metadata now clamp passthrough/auto SceneTracks to explicit fallback semantics instead of marking them non-stub or non-heuristic; only real SceneTracks stay eligible for grounded/non-stub truth."
    },
    {
      "id": "bootstrap_workcell_trace_completeness",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - bootstrap workcell episodes now emit canonical per-episode runtime packet / event spine / decision ledger artifacts, replay import now discovers those refs directly, and bootstrap summaries now distinguish trace-complete rows from real-SAM3D grounded-data readiness."
    },
    {
      "id": "workcell_coverage_graph_mapping",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - the coverage harvester now canonicalizes workcell env/task/skill ids and the skill graph now includes a built-in peg_in_hole workcell chain, so workcell bootstrap rows land in the canonical coverage graph instead of collapsing to task-only evidence."
    },
    {
      "id": "shadow_advisory_sampling_learning",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - shadow advisory now auto-consumes canonical semantic-runtime scorer runtime packages when present, preserves contract type / promotion stage / benchmark-gate truth, threads bounded learned route/regret/counterfactual/authority signals into replay sampling, and keeps the heuristic path only as an explicit fallback when no scorer package exists yet."
    },
    {
      "id": "semantic_coverage_helper_runtime_contracts",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - semantic feedback adapters and semantic WM refiners now have canonical runtime-package contracts plus bounded disabled|auto|required helper loading in coverage_loop/pipeline_manager, replacing raw checkpoint blobs and implicit shadow-fit-only behavior."
    },
    {
      "id": "scene_tracks_truth_consumer_sweep",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - Stage 1 routing, local synthetic branch metadata, synthetic corpus benchmark gating, live semantic runtime scoring, and degraded-fusion artifacts now all share the same SceneTracks truth normalization, and the SceneTracks runner now emits explicit GPU + SAM3D host preconditions for real grounded-data readiness."
    },
    {
      "id": "semantic_policy_datapack_selection",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - semantic policy selection now ranks datapacks using ARH-adjusted historical outcomes, candidate quality/novelty, benchmark/readiness support, and explicit gap-fill pressure; semantic simulation now records the resulting selection summary into live results and run logs."
    },
    {
      "id": "shadow_advisory_scorer_preconditions",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - shadow advisory now emits explicit semantic-runtime scorer precondition artifacts and blocking work orders when no scorer package is available, and the main advisory/training consumers persist those artifacts into runtime outputs instead of hiding the fallback inside branch logic."
    },
    {
      "id": "vla_labeled_datapack_truth_contract",
      "completed_at": "2026-03-26",
      "notes": "P2 complete - DatapackConfig/registry now preserve metadata plus quality/novelty across YAML and ontology, rollout labeling aggregates teacher-runtime/SceneTracks/provenance/precondition truth into labeled datapacks, and semantic simulation enriches those datapacks with semantic-fusion artifact refs and readiness instead of dropping vision semantics as sidecars."
    },
    {
      "id": "semantic_conditioning_truth_bridge",
      "completed_at": "2026-03-26",
      "notes": "P2 complete - semantic runtime backbone now emits compact runtime-truth / benchmark / execution-precondition summaries, and observation/condition-vector builders now preserve and react to those signals so benchmark-unready grounding or blocked fusion changes conditioning instead of remaining metadata-only."
    },
    {
      "id": "gen2sim_validity_value_admission",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - local synthetic branches now emit explicit gen2sim validity sidecars, corpus/trainer policy now reacts materially to admission truth, Regal datapack valuation now resolves the explicit assessment plus a bounded learned helper package, and the learned helper remains honestly benchmark-gated on empirical receipt density."
    },
    {
      "id": "semantic_selection_meta_choice_receipts",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - semantic selection now exports a real bounded neural helper package, persists per-episode selection_summary sidecars into rollout artifacts, replay ingest preserves those summaries into runtime rows, and orchestration context encoding now reacts to selection-policy/helper-status/meta-choice features instead of dropping them after the sim run log."
    },
    {
      "id": "orchestration_sequence_contract",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - orchestration transformer/runtime now use bounded multi-step tool logits with an explicit PAD/stop label, runtime planning records model sequence traces before bounded heuristic backfill, and the trainer/eval path now optimizes and reports full bounded tool-sequence behavior instead of only the first routing decision."
    },
    {
      "id": "meta_transformer_planning_helper",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - meta-transformer runtime/training now carry a shared planning-context contract over semantic/econ/datapack/selector receipts, the trained helper now predicts bounded objective/backend/energy-profile/data-mix/expected-delta outputs, and runtime records both planning traces and the bounded planning_application receipt explaining why the final meta-choice stayed heuristic-prior or moved toward the learned helper."
    },
    {
      "id": "knob_model_homeostatic_helper",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - removed the fake D4 learned stub, added a canonical knob-model trainer/runtime-package lane with runtime receipt ingestion, wired bounded benchmark-gated helper loading into the homeostatic planner, and preserved knob-policy regime/base-config receipts in GateStatus plus closed-loop smoke artifacts."
    },
    {
      "id": "semantic_orchestrator_shell_policy",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - SemanticOrchestratorV2 now loads a real bounded shell-policy helper package trained on semantic snapshot plus orchestrator-advisory receipts, blends learned preset/strategy/safety/activation outputs against the explicit heuristic prior with disabled|auto|required semantics, and preserves policy-source/promotion/helper-trace receipts in runtime metadata."
    },
    {
      "id": "pipeline_manager_stage_activation_policy",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - PipelineManager now loads a real bounded stage-policy helper package trained on pipeline-state receipts, preserves explicit stage-policy traces, reorders activated stages by bounded learned priority, and lets the helper influence next-iteration config suggestions while keeping shell activation hard-gated by execution readiness."
    },
    {
      "id": "queue_dispatch_reweight_policy",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - queue selection now loads a real bounded queue-dispatch helper package, blends learned dispatch scores against the explicit heuristic multiplier prior, preserves queue-policy traces on live decisions, and the main RL/shadow training entrypoints can pass that helper into the actual replay-selection loop."
    },
    {
      "id": "sampler_base_weight_strategy_policy",
      "completed_at": "2026-03-26",
      "notes": "P1 complete - DataPackRLSampler now loads a real bounded sampler-policy helper package, blends learned strategy selection, per-episode base weights, and frontier/econ threshold/focus parameters against explicit heuristic priors, preserves sampler traces in sampling metadata, and the main shadow/online training entrypoints persist sampler-policy receipts into runtime outputs."
    }
  ],
  "backlog": [
    {
      "id": "sampler_policy_receipt_density_for_promotion",
      "priority": "P2",
      "blocked_by": "queue outcome receipts and replay counterfactual labels",
      "notes": "The sampler-policy helper is now wired into live sampling and training entrypoints, but it still trains mostly on heuristic-bootstrap receipts and needs denser queue outcome / counterfactual labels before it can promote beyond shadow_candidate."
    },
    {
      "id": "semantic_runtime_scorer_density_for_promotion",
      "priority": "P2",
      "blocked_by": "execution-ready and semantic-grounded replay density",
      "notes": "The semantic runtime scorer now has a canonical trainer/runtime package and shadow-advisory consumer contract, but it still needs more execution-ready, semantic-grounded replay rows before it can promote beyond shadow_candidate."
    },
    {
      "id": "semantic_coverage_helper_artifact_density_for_promotion",
      "priority": "P2",
      "blocked_by": "repeated coverage-loop artifact accumulation",
      "notes": "The semantic feedback adapter and semantic WM refiner now have canonical runtime-package contracts, but they still need more repeated coverage-loop artifacts and non-trivial correction/proposal histories before they can promote beyond shadow_candidate."
    },
    {
      "id": "real_sam3d_grounded_data_refresh",
      "priority": "P1",
      "blocked_by": "GPU host with SAM3D checkpoints and real workcell refresh runs",
      "notes": "Grounded-data honesty is fixed, but benchmark-grade workcell/vision promotion still cannot happen until SceneTracks/SAM3D runs produce real grounded data rather than passthrough fallback outputs."
    },
    {
      "id": "gen2sim_receipt_density_for_promotion",
      "priority": "P2",
      "blocked_by": "real runtime synth receipts and downstream datapack outcome labels",
      "notes": "The gen2sim validity helper is wired and benchmark-gated, but it still needs empirical receipt density before it can promote beyond shadow_candidate and stop leaning on distilled explicit assessments."
    }
  ]
}
